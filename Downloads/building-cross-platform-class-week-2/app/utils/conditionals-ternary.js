/*
 * export a default function which takes two numbers,
 * and if the first is greater than the second, it returns
 * the first, else the second.
 *
 * Use a ternary operator to do this.
 */

export function ternary(a, b) {
  var s = a > b ? b : a;
  return s;
}
